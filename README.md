
  # MVP with MySQL Integration

  This is a code bundle for MVP with MySQL Integration. The original project is available at https://www.figma.com/design/2VTBejyYpaiPFFGqinSLD8/MVP-with-MySQL-Integration.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  